apiVersion: v1
kind: Service
metadata:
  name: gb-mysql
spec:
  ports:
  - port: 3306
  selector:
    app: gb-db
---
apiVersion: v1
kind: Service
metadata:
  name: gb-web
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: gb-web 